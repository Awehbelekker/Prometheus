tunnel: prometheus-unified
credentials-file: C:\Users\Judy\.cloudflared\5ad70171-4c9d-4cd0-8c83-93031bb10484.json

ingress:
  # Frontend Dashboard - Main Interface
  - hostname: prometheus-trader.com
    service: http://localhost:3000
    originRequest:
      httpHostHeader: localhost:3000
      connectTimeout: 30s
      tlsTimeout: 30s
      tcpKeepAlive: 30s
      keepAliveConnections: 100
      keepAliveTimeout: 90s

  # WWW subdomain
  - hostname: www.prometheus-trader.com
    service: http://localhost:3000
    originRequest:
      httpHostHeader: localhost:3000
      connectTimeout: 30s
      tlsTimeout: 30s
      tcpKeepAlive: 30s
      keepAliveConnections: 100
      keepAliveTimeout: 90s

  # Alternative frontend access
  - hostname: app.prometheus-trader.com
    service: http://localhost:3000
    originRequest:
      httpHostHeader: localhost:3000
      connectTimeout: 30s
      tlsTimeout: 30s

  # API Backend - For AJAX calls and REST endpoints
  - hostname: api.prometheus-trader.com
    service: http://localhost:8000
    originRequest:
      httpHostHeader: localhost:8000
      connectTimeout: 30s
      tlsTimeout: 30s
      tcpKeepAlive: 30s
      keepAliveConnections: 100
      keepAliveTimeout: 90s

  # WebSocket connections for real-time data
  - hostname: ws.prometheus-trader.com
    service: http://localhost:8000
    originRequest:
      httpHostHeader: localhost:8000
      connectTimeout: 30s
      tlsTimeout: 30s
      tcpKeepAlive: 30s
      keepAliveConnections: 100
      keepAliveTimeout: 90s

  # Admin Dashboard - Separate subdomain for admin access
  - hostname: admin.prometheus-trader.com
    service: http://localhost:3000
    originRequest:
      httpHostHeader: localhost:3000
      connectTimeout: 30s
      tlsTimeout: 30s

  # Trading Dashboard - Direct trading interface
  - hostname: trade.prometheus-trader.com
    service: http://localhost:3000
    originRequest:
      httpHostHeader: localhost:3000
      connectTimeout: 30s
      tlsTimeout: 30s

  # Documentation and Help
  - hostname: docs.prometheus-trader.com
    service: http://localhost:8000
    originRequest:
      httpHostHeader: localhost:8000
      connectTimeout: 30s
      tlsTimeout: 30s

  # Catch-all rule (required)
  - service: http_status:404
